# راهنمای استقرار در پارس پک (Pars Pack Deployment Guide)

راهنمای ساده و سریع برای استقرار Django در پارس پک

---

## فایل‌های مورد نیاز ✅

پروژه شما آماده است و شامل:

- ✅ `runtime.txt` - نسخه Python (3.11)
- ✅ `Procfile` - دستورات اجرا
- ✅ `requirements.txt` - وابستگی‌ها
- ✅ `.env.example` - نمونه متغیرهای محیطی

---

## مراحل استقرار

### 1️⃣ آماده‌سازی متغیرهای محیطی

فایل `.env` خود را ایجاد کنید:

```bash
cp .env.example .env
```

سپس این متغیرها را تنظیم کنید:

#### `SECRET_KEY` - تولید کلید امنیتی

یک SECRET_KEY تصادفی بسازید:

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

یا:

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(50))"
```

#### `DEBUG`
```env
DEBUG=False
```

#### `ALLOWED_HOSTS`
بعد از ایجاد اپلیکیشن در پارس پک، دامنه را اضافه کنید:
```env
ALLOWED_HOSTS=myapp.parspack.app
```

#### `DATABASE_URL`
در پنل پارس پک یک PostgreSQL ایجاد کنید، سپس:
```env
DATABASE_URL=postgresql://username:password@host:port/database
```

#### `REDIS_URL`
در پنل پارس پک یک Redis ایجاد کنید، سپس:
```env
REDIS_URL=redis://host:port/0
```

---

### 2️⃣ استقرار در پارس پک

#### روش 1: آپلود مستقیم

1. وارد [پنل پارس پک](https://parspack.com) شوید
2. روی **ایجاد اپلیکیشن جدید** کلیک کنید
3. **Python/Django** را انتخاب کنید
4. کد خود را آپلود کنید
5. متغیرهای محیطی را از `.env` وارد کنید
6. روی **استقرار** کلیک کنید

#### روش 2: اتصال به Git

1. کد را به GitHub/GitLab پوش کنید
2. در پنل پارس پک، **استقرار از Git** را انتخاب کنید
3. Repository را وصل کنید
4. متغیرهای محیطی را تنظیم کنید
5. **استقرار** را بزنید

---

### 3️⃣ تنظیمات سرویس‌ها

#### PostgreSQL
- در پنل → **Databases** → **PostgreSQL جدید**
- نام: `accounts_db` (یا هر نامی)
- اطلاعات اتصال را در `DATABASE_URL` بگذارید

#### Redis
- در پنل → **Databases** → **Redis جدید**
- اطلاعات اتصال را در `REDIS_URL` بگذارید

---

## چک‌لیست قبل از استقرار

- [ ] `SECRET_KEY` تولید شده (50+ کاراکتر)
- [ ] `DEBUG=False` تنظیم شده
- [ ] `ALLOWED_HOSTS` شامل دامنه پارس پک
- [ ] PostgreSQL ساخته شده و `DATABASE_URL` تنظیم شده
- [ ] Redis ساخته شده و `REDIS_URL` تنظیم شده
- [ ] `.env` در `.gitignore` هست (برای Git deployment)

---

## تست محلی

قبل از استقرار، تست کنید:

```bash
# نصب وابستگی‌ها
pip install -r requirements.txt

# مایگریشن
python manage.py migrate

# جمع‌آوری فایل‌های استاتیک
python manage.py collectstatic --noinput

# اجرای سرور
gunicorn config.wsgi:application
```

---

## نکات مهم ⚠️

### امنیت
- هرگز `DEBUG=True` در production نگذارید
- `SECRET_KEY` را امن نگه دارید
- `.env` را commit نکنید

### عملکرد
- حتماً PostgreSQL استفاده کنید (نه SQLite)
- Redis برای cache فعال کنید
- WhiteNoise برای فایل‌های استاتیک نصب است

---

## عیب‌یابی

### خطای 500
- لاگ‌ها را در پنل بررسی کنید
- `DEBUG=False` و `ALLOWED_HOSTS` را چک کنید

### خطای Database
- `DATABASE_URL` را بررسی کنید
- مطمئن شوید PostgreSQL روشن است

### فایل‌های استاتیک نمایش داده نمی‌شوند
- `Procfile` شامل `collectstatic` است
- تنظیمات WhiteNoise را چک کنید

---

## پشتیبانی

- [مستندات پارس پک](https://docs.parspack.com)
- [پشتیبانی پارس پک](https://parspack.com/support)

---

**موفق باشید! 🚀**
